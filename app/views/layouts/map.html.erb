<!DOCTYPE html>
<html>
  <head>  
      <title>TaskAssure [<%=  yield(:title)  %>]</title>
      <%= favicon_link_tag 'favicon.ico' %>
      <%= stylesheet_link_tag "application"  %>
      <%= javascript_include_tag "application"%>
     
      <%= csrf_meta_tags %>
      <%= yield :head %>
  </head>
  <body>
    
          <%= yield %>
    
 <%= yield :styles %>
      <%= yield :scripts %>
  </body>
</html>
